<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Online Clipboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 flex flex-col items-center justify-center text-white"
  >
    <!-- Navbar -->
    <nav
      class="w-full bg-gray-800 p-4 flex justify-between items-center shadow-md"
    >
      <h1 class="text-white text-xl font-bold">Clipboard</h1>
      <div class="flex items-center gap-4">
        <a href="#" class="text-white hover:underline">Home</a>
        <a href="#" class="text-white hover:underline">About</a>
        <a href="#" class="text-white hover:underline">Contact</a>
      </div>
    </nav>

    <div class="w-full max-w-md bg-gray-800 p-6 rounded-lg shadow-md mt-6">
      <h1 class="text-2xl font-bold text-center text-blue-400">
        Online Clipboard
      </h1>

      <!-- Display Message (Success or Error) -->
      <% if (message) { %>
      <p
        class="<%= messageType === 'success' ? 'text-green-400' : 'text-red-400' %> font-semibold text-center my-2"
      >
        <%= message %>
      </p>
      <% } %>

      <!-- Form to Save Text -->
      <form id="saveForm" action="/save" method="POST" class="mt-4">
        <textarea
          name="text"
          class="w-full p-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 h-40 bg-gray-700 text-white"
          placeholder="Paste your text here..."
          required
        ></textarea>

        <button
          type="submit"
          class="w-full mt-3 bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 transition flex justify-center items-center"
        >
          Generate Code
          <span
            id="saveLoader"
            class="hidden ml-2 h-5 w-5 border-2 border-white border-t-transparent rounded-full animate-spin"
          ></span>
        </button>
      </form>

      <hr class="my-4 border-gray-600" />

      <!-- Retrieve Section -->
      <h2 class="text-lg font-semibold text-white text-center">
        Retrieve Data
      </h2>
      <% if (retrievedText) { %>
      <p class="mt-2 text-white font-medium">Retrieved Data:</p>
      <textarea
        id="retrievedText"
        class="w-full p-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 h-40 bg-gray-700 text-white"
        readonly
      >
<%= retrievedText %></textarea
      >

      <button
        onclick="copyToClipboard()"
        class="w-full mt-2 bg-green-500 text-white py-2 rounded-md hover:bg-green-600 transition"
      >
        Copy to Clipboard
      </button>
      <p id="copyMessage" class="text-green-400 text-center mt-2 hidden">
        Copied!
      </p>
      <% } %>

      <!-- Form to Retrieve Text -->
      <form id="retrieveForm" action="/retrieve" method="POST" class="mt-4">
        <input
          type="text"
          name="code"
          class="w-full p-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 bg-gray-700 text-white"
          placeholder="Enter 4-digit code"
          required
        />
        <button
          type="submit"
          class="w-full mt-3 bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 transition flex justify-center items-center"
        >
          Retrieve
          <span
            id="retrieveLoader"
            class="hidden ml-2 h-5 w-5 border-2 border-white border-t-transparent rounded-full animate-spin"
          ></span>
        </button>
      </form>
    </div>

    <script>
      function copyToClipboard() {
        const textArea = document.getElementById("retrievedText");
        textArea.select();
        document.execCommand("copy");

        document.getElementById("copyMessage").classList.remove("hidden");
        setTimeout(() => {
          document.getElementById("copyMessage").classList.add("hidden");
        }, 3000);
      }

      document
        .getElementById("saveForm")
        .addEventListener("submit", function () {
          document.getElementById("saveLoader").classList.remove("hidden");
        });

      document
        .getElementById("retrieveForm")
        .addEventListener("submit", function () {
          document.getElementById("retrieveLoader").classList.remove("hidden");
        });
    </script>
  </body>
</html>
